import Axios from 'axios'
import React, { useState, useEffect } from 'react'
import axios from 'axios'

const Recipe = (props) => {
    console.log(props.match.params,'propsparams')

    const [ recipe, setRecipe ] = useState({})


    useEffect(() => {
        
    })

    return (
        <div>
            
        </div>
    )
}

export default Recipe
